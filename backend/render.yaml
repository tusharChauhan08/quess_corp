[build]
  command = "pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port 8000"
  functions = { api = "backend" }

[env]
  PYTHONUNBUFFERED = "1"
  DATABASE_URL = "postgresql://user:password@your-db-host:5432/hrms_lite"
